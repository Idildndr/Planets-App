<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="android.example.planetss.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main"/>
    <action
        android:id="@+id/action_mainActivity2_to_earthFragment"
        app:destination="@id/earthFragment" />
    <fragment

        android:id="@+id/earthFragment"
        android:name="android.example.planetss.EarthFragment"
        android:label="fragment_earth"
        tools:layout="@layout/fragment_earth" >

    </fragment>
</navigation>